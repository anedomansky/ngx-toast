import { ToastContainerDirective } from './toast-container.directive';

describe('ToastContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ToastContainerDirective();
    expect(directive).toBeTruthy();
  });
});
